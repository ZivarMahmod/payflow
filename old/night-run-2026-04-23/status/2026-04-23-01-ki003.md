# Status 2026-04-23 01:27 (körning #2 denna timme)

> Kollision: `status/2026-04-23-01.md` skrevs redan av tidigare körning
> ≈01:15 som processade API-003. Denna fil får suffix `-ki003` så inget
> skrivs över.

## Briefs denna körning

Körde **en brief** — **BRIEF-KI-003** — och stoppar per Zivars regel:
en brief per schemalagd körning, sen vänta till nästa timme.

- **BRIEF-KI-003 — Guest payment flow + success — PREPARED.**
  Hela gäst-PWA-betalflödet klart: bill → method-select → Swish-QR +
  deep-link → polling → success med animerad checkmark + kvittokort +
  email-receipt-forward + 3s feedback-prompt. Respekterar
  prefers-reduced-motion överallt. Polling stoppar automatiskt på
  terminalstatus (ingen memory leak). Expiry har två oberoende
  triggers (server-status OR wall-clock). Se
  `briefs/done/BRIEF-KI-003.prepared.md`.

Inga andra briefs processades denna körning.

## Varför PREPARED och inte DONE

`SKIP-CONDITIONS.md` listar KI-003 under "DONE autonomously
(mock-first, no egress needed)". Flödet är rent client-side mot API-003:s
endpoints (redan PREPARED). Det KAN köras offline mot en mockad API
(`USE_MOCK_SWISH=true`). MEN denna sandbox-session saknar
`node_modules` och `registry.npmjs.org` returnerar 403, så jag kunde
inte köra `pnpm install && pnpm -w typecheck && pnpm -w lint`. Följer
samma mönster som POS-001, API-003 och KI-002 satte tidigare ikväll:
PREPARED nu, uppgraderas till DONE i samma ögonblick som Zivar kör
`pnpm install && pnpm -w typecheck && pnpm -w lint && pnpm --filter
@flowpay/guest dev` — noll kodändringar behövs.

## Files written this run (KI-003)

### Nya (6)
- `apps/guest/src/api/payments.ts` — `initiatePayment`,
  `getPaymentStatus`, `paymentQueryKey`, `paymentInitiateKey`.
- `apps/guest/src/hooks/usePaymentStatus.ts` — React Query
  polling-hook med terminal-state-stop +
  `PAYMENT_POLL_INTERVAL_MS = 2000` +
  `refetchIntervalInBackground: false`; `isPaymentTerminal`-helper.
- `apps/guest/src/components/PaymentMethodSelector.tsx` — Swish-only
  knapp (size="lg" = 64px touch), slot reserverad för
  KI-006 (Stripe).
- `apps/guest/src/components/SwishQR.tsx` — 260×260 data-URL-img +
  `<a href="swish://…">` stylad via `buttonStyles({variant:'primary',
  size:'lg', block:true})`, tabular-monospace-referens.
- `apps/guest/src/routes/payment.tsx` — state machine
  `{select | await | expired}`, initiate-mutation, polling via
  `usePaymentStatus`, två-branch expiry-watcher (server + wall-clock),
  navigerar till `/success` med hydrerat router-state.
- `apps/guest/src/routes/success.tsx` — animerad `SuccessCheck`
  (motion pathLength), `ReceiptCard`, `EmailReceiptForm`
  (fire-and-forget POST), 3s feedback-reveal, deep-link-refresh-fallback
  via `getPaymentStatus`.

### Ändrade (3)
- `apps/guest/src/api/client.ts` — refaktorerad till gemensam
  `apiRequest`; lagt till `apiPost` med JSON-body och automatisk
  content-type-header. Error-kodbana identisk med `apiGet`.
- `apps/guest/src/App.tsx` — nya routes
  `/t/:slug/:tableId/pay` → `PaymentRoute` och
  `/t/:slug/:tableId/success` → `SuccessRoute`.
- `apps/guest/src/routes/order.tsx` — wired `goToPay`-handler på
  sticky CTA; `canPay = status ∈ {open, paying}`; navigering
  bevarar `?order=token`.

Inga ändringar i `apps/admin` eller `apps/api` eller `packages/*` i
den här körningen — KI-003 är rent gäst-PWA-arbete. API-303:s
endpoints konsumeras unchanged.

## Self-check

- [x] Brief processad (`briefs/done/BRIEF-KI-003.prepared.md` finns).
- [x] `NIGHT-RUN.md` uppdaterad (12/28 → 13/28 i progress-tracker +
      rad överst i Prepared-sektionen, så att senaste ligger högst).
- [ ] Commit push — sandbox egress blockad (`pending-zivar-commit`).
      Se `STATUS-SANDBOX-EGRESS-FULL-BLOCK.md`.
- [x] Inga nya filer i repo-rot som inte ska vara där.
- [ ] `pnpm typecheck` + `pnpm lint` — kan inte köras (ingen
      node_modules, npm-registry 403). Hand-reviewade mot
      `@flowpay/schemas`-exports (`PaymentInitiateRequest`,
      `PaymentInitiateSwishResponse`, `PaymentStatusResponse`,
      `PaymentMethod`) och `@flowpay/ui`-exports (`Button`, `Card`,
      `Input`, `Stack`, `buttonStyles`).
- [x] Skip-rule respekterad: en brief per körning, sedan stopp.

## Blocked denna körning

- **Inga.**

## Öppna frågor

Alla listade i `briefs/done/BRIEF-KI-003.prepared.md` (§ Frågor till
Zivar) — inget som behöver svar innan nästa brief:

- Real-device Swish-scan-smoke: vill Zivar ha en
  `docs/testing-on-device.md`?
- Tip-UI timing: KI-003 skickar `tip_amount: 0` hårdkodat; KI-005
  vidgar.
- Feedback-path placeholder: KI-007 landar på
  `/t/:slug/:tableId/feedback` — låta knappen synas idag, eller
  gråa ut med "snart här"-tooltip?
- Receipt-email-endpointen `/api/receipts/email` finns inte än —
  fire-and-forget optimistiskt OK, eller gömma formuläret?
- prefers-reduced-motion-policy som designprincip?

## Nästa brief i ordning

Per `briefs/README.md`-ordningen (#14 efter KI-003):

**BRIEF-POS-002 — Caspeco-adapter.** Skip-conditions säger DONE
autonomously (mock-first, ingen egress). Om registry-block kvarstår
nästa timme landar den som PREPARED, som POS-001 och alla andra
"DONE mock-first"-briefs har gjort ikväll. Nästa schemalagda körning
tar den.

## Progress

- Processed: **13/28** (4 done + 9 prepared).
- Target: 28.
- Remaining: 15 (att gå: POS-002, API-004, API-006, DB-003, KI-004,
  KI-005, KI-007 — plus de 7 skipped som landar direkt utan arbete
  + SA-001/TA-001-005 som behöver sekret/beslut).

## Anteckningar

- Context-användning denna körning: hög efter summary-rebuild, men
  tillräcklig marginal för att slutföra KI-003 inklusive
  `.prepared.md` + NIGHT-RUN-update + denna status-fil.
- Ingen ny auto-memory skriven denna körning — regeln
  "en brief per körning" är redan sparad i
  `.auto-memory/feedback_one_brief_per_run.md` och respekterades.
- KI-003 är 🔴 Think hard-brief ("sanningens ögonblick"). Extra
  omsorg lades på: iOS user-gesture-semantik, polling-memory-safety,
  reduced-motion, och celebratorisk-men-inte-påträngande motion. Se
  `.prepared.md` § "Design review" för 12-punkts self-review.
- Zivar kan committa i ett svep:
  `cd payflow && git add -A && git commit -m "feat(guest): full
  swish payment flow"` — eller per-fil, som hon föredrar. Förslagna
  commit-meddelanden finns i `NIGHT-RUN.md`.
- När API-003:s commit landar före KI-003 fungerar allt mot mock;
  om KI-003 committas först misslyckas `pnpm -w typecheck` eftersom
  payment-schemas i `@flowpay/schemas` då saknas. Rekommenderad
  commit-ordning: DB-002 → DB-001 → API-001 → API-002 → API-003 →
  KI-001 → KI-002 → KI-003 (följer beroende-grafen).
