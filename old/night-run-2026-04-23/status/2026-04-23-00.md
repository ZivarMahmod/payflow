# Status 2026-04-23 00:00 (körning som landade ≈02:45 lokal tid)

## Briefs denna körning

Körde **en brief** — **BRIEF-KI-002** — och stoppade per Zivars nya
regel: "kör inte gärnet, när du är klar med en brief vänta till nästa
timma sne kör igång igen."

- **BRIEF-KI-002 — Visa nota med riktig data — PREPARED.**
  Gäst-PWA:n är nu kopplad till `GET /orders/:token` via TanStack Query
  med premium skeleton + per-kod error state + staggered Framer Motion.
  Se `briefs/done/BRIEF-KI-002.prepared.md`.

Inga andra briefs processades denna körning.

## Not om tidigare batch-körningar

Innan Zivars regelpåminnelse hade samma "session 2026-04-23" redan
processat flera briefs (DB-001/002, SC-001, API-001, POS-001, API-002).
Dessa ligger alla i `NIGHT-RUN.md` under Prepared och är commit-färdiga
på Zivars maskin. Batch-körningen gick mot egress-block och inget är
pushat; alla hashar är `pending-zivar-commit`. Framöver = en brief per
timme enligt schemat.

## Files written this run (KI-002)

- `apps/guest/src/api/client.ts` — fetch wrapper + `ApiError`/`ApiErrorCode`.
- `apps/guest/src/api/orders.ts` — `getOrder(token, signal)` + `orderQueryKey`.
- `apps/guest/src/components/OrderSkeleton.tsx` — CSS shimmer, reduced-motion safe.
- `apps/guest/src/components/OrderError.tsx` — per-code Swedish copy + retry button.
- `apps/guest/src/routes/order.tsx` — rewritten with useQuery, dropped KI-001 DUMMY fixture, added sticky pay-CTA.
- `apps/guest/src/lib/format.ts` — added `formatAmount(amount, currency)` for decimal SEK from the API.
- `apps/guest/.env.example` — documents `VITE_API_URL`.

Inga ändringar i `packages/` eller `apps/api/` denna körning. TypeScript
har ingen egen typning i `apps/guest` utanför det som redan finns i
`@flowpay/schemas` (exporterat från API-002).

## Self-check

- [x] Brief processad (`briefs/done/BRIEF-KI-002.prepared.md` finns).
- [x] `NIGHT-RUN.md` uppdaterad (10/28 → 11/28 i progress tracker, rad i
      Prepared-sektionen).
- [ ] Commit push — sandbox egress blockad. Placeholder
      `pending-zivar-commit`. Se `STATUS-SANDBOX-EGRESS-FULL-BLOCK.md`.
- [x] Inga nya filer i repo-rot som inte ska vara där.
- [ ] `pnpm typecheck` + `pnpm lint` — kan inte köras (npm-registry 403).
      Hand-reviewade mot `@flowpay/schemas` exporten som API-002 landade.

## Blocked denna körning

- **Inga.**

## Öppna frågor

Alla listade i `briefs/done/BRIEF-KI-002.prepared.md` (§ Frågor till
Zivar) — inget som behöver svar innan nästa brief:

- VITE_API_URL i prod (Fly.io app-namn ej valt).
- CTA-disabled copy fram tills KI-003 landar.
- Skeleton-rader: 3 st. Ev. tweak efter real-device QA.

## Nästa brief i ordning

**BRIEF-API-003 — Swish-flöde.** Per `briefs/README.md` ordning (#12),
efter KI-002 (#11). Skip-conditions säger Done (mock-first, inga
credentials behövs). Nästa schemalagda körning tar den.

## Progress

- Processed: **11/28** (4 done + 7 prepared).
- Target: 28.
- Remaining: 17 (2 Prepared kvar, 7 Done-mock-first kvar, 7 Skipped, 1 DB-003 Prepared kvar — räkna per README).

## Anteckningar

- Context-användning denna körning: måttlig. Batch-körningen innan
  Zivars regelpåminnelse åt större delen.
- Sparade feedback-memory:
  `/sessions/upbeat-loving-knuth/mnt/.auto-memory/feedback_one_brief_per_run.md`
  med regeln "en brief per schemalagd körning, sen stopp." Kommer
  läsas vid nästa schemalagda trigger.
- Zivar kan köra allt från `pending-zivar-commit` i ett svep: `cd
  payflow && git add -A && git commit -m "feat(guest): connect to
  orders API"` — eller brief för brief, som hon föredrar.
