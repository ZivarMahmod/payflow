# Status 2026-04-23 02:15 (körning #3 denna timme-cykel)

## Briefs denna körning

Körde **en brief** — **BRIEF-API-004** — och stoppar per Zivars regel:
en brief per schemalagd körning, sen vänta till nästa timme.

- **BRIEF-API-004 — Mark-order-paid → POS — PREPARED.**
  Hela "berätta för POS:en att notan är betald"-pipelinen klar:
  durable queue (`pos_update_queue`) med UNIQUE(payment_id, action)
  som idempotency-storlås, AFTER UPDATE-trigger på `payments` som
  auto-enqueuear när `status` flippar till `completed`,
  worker-RPC:er med `FOR UPDATE SKIP LOCKED` för parallellsäkra
  replicas + lease-baserad crash-recovery, exponentiell backoff i
  worker:n (5 s, 30 s, 2 min, 10 min, 1 h), automatisk
  admin-notify-callback efter 5:e misslyckandet. Se
  `briefs/done/BRIEF-API-004.prepared.md` för design-review (12
  punkter) och Zivars manuella verifieringssteg.

Inga andra briefs processades denna körning.

## Varför PREPARED och inte DONE

`SKIP-CONDITIONS.md` listar API-004 explicit som **PREPARED**
(EGRESS_BLOCKED, manuell verifiering = "End-to-end test with local
API + cloud DB"). All kod/SQL är skriven; det enda som inte kan köras
i sandbox är: (a) live-anrop mot Supabase för migration-push, och (b)
end-to-end-smoke mot lokala API:n + cloud-DB. Dessutom saknar
sandboxen `node_modules` och `registry.npmjs.org` returnerar 403, så
även `pnpm -w typecheck && pnpm -w lint` är deferred. Följer samma
mönster som POS-001, API-003, KI-002, KI-003.

## Files written this run (API-004)

### Nya (4)
- `packages/db/supabase/migrations/20260423000007_pos_update_queue.sql`
  — `pos_update_queue`-tabell + 3 indexes (partial pending,
  restaurant_status, processing-lease) + RLS (staff SELECT only) +
  `payments_enqueue_pos_update` AFTER UPDATE-trigger +
  `claim_pos_update_queue_items(p_limit, p_lease_seconds)` worker
  dequeue + `complete_pos_update_queue_item(p_id)` happy-path
  finalise + `fail_pos_update_queue_item(p_id, p_error,
  p_next_attempt_at, p_max_attempts)` retry/finalise.
- `apps/api/src/services/pos-update-queue.ts` — `PosUpdateQueueWorker`
  klass (setInterval-driven loop, jittered start, process-level backoff
  på consecutive RPC-failures, per-row error-isolation) +
  `POS_UPDATE_BACKOFF_MS` + `POS_UPDATE_MAX_ATTEMPTS` + `processOnce()`
  + `start()`/`stop()` lifecycle + `notifyAdminOnFail` injection point.
- `apps/api/src/services/payment-completion.ts` — `completePayment()`
  thin idempotent wrapper för framtida webhook-/admin-konsumenter.
  `PaymentCompletionError` med typade koder
  (`NOT_FOUND` | `BAD_STATE` | `DB_ERROR`).
- `apps/api/src/services/pos-update-queue.test.ts` — vitest-svit, 6
  cases (happy, empty batch, transient error retry, 5:e
  failure → admin notify, orphan integration, complete-RPC fel efter
  lyckad POS-call). Inget riktigt Supabase — in-memory stub för de
  fyra RPC:erna worker:n använder.

### Ändrade (2)
- `packages/db/src/database.types.ts` — lagt till `pos_update_queue`
  Tables-entry + de tre nya Functions
  (`claim_pos_update_queue_items`, `complete_pos_update_queue_item`,
  `fail_pos_update_queue_item`); row helper-aliases
  (`PosUpdateQueueRow`, `PosUpdateQueueInsert`, `PosUpdateQueueUpdate`);
  bumpat migration-list-kommentaren med 007.
- `apps/api/src/server.ts` — startar `PosUpdateQueueWorker` när
  `ENABLE_POS_SYNC=true` (samma flag som sync-schedulern, samma
  worker-deployment), graceful stop på `onClose`.

Inga ändringar i `apps/admin`, `apps/guest`, `packages/schemas` eller
`packages/pos-adapters` denna körning — API-004 är rent api+db-arbete.

## Self-check

- [x] Brief processad (`briefs/done/BRIEF-API-004.prepared.md` finns).
- [x] `NIGHT-RUN.md` uppdaterad (13/28 → 14/28 i progress-tracker +
      ny rad överst i Prepared-sektionen).
- [ ] Commit push — sandbox egress blockad (`pending-zivar-commit`).
      Se `STATUS-SANDBOX-EGRESS-FULL-BLOCK.md`.
- [x] Inga nya filer i repo-rot som inte ska vara där.
- [ ] `pnpm typecheck` + `pnpm lint` — kan inte köras (ingen
      node_modules, npm-registry 403). Hand-reviewade mot:
        - `@flowpay/pos-adapters`-exports (`POSProvider`,
          `POSMarkPaidInput`, `POSAdapterError`, `getPOSProvider`,
          `PosType`)
        - `@flowpay/db`-types (`Database`, nya `pos_update_queue`-row
          + tre nya RPC:er)
        - befintliga services (`PaymentExpirerScheduler`,
          `SyncScheduler`, `PosSyncService`) för konsekventa
          option-shapes och logger-kontrakt.
- [x] Skip-rule respekterad: en brief per körning, sedan stopp.

## Blocked denna körning

- **Inga.**

## Öppna frågor

Alla listade i `briefs/done/BRIEF-API-004.prepared.md` (§ Frågor till
Zivar) — inget som behöver svar innan nästa brief:

- Email-provider för admin-notifications när 5:e retry brinner?
- Backoff-schemat — re-tune efter första riktiga Onslip-utfall?
- Splitta `ENABLE_POS_SYNC` → `ENABLE_POS_SYNC` + `ENABLE_POS_QUEUE`?
- Lease-längd 60 s — sänka till 15 s för snabbare crash-recovery?
- `completePayment()`-konsumenter: bekräfta POS-003 (Stripe webhook)
  som nästa caller?
- Failed-row TTL / cleanup-job?

## Nästa brief i ordning

Per `briefs/README.md`-ordningen (#15 efter API-004):

**BRIEF-KI-004 — Split-flöde.** `SKIP-CONDITIONS.md` listar KI-004
under "DONE autonomously (mock-first, ingen egress)". Om
registry-blocket kvarstår nästa timme landar den som PREPARED enligt
samma mönster som alla andra mock-first-briefs ikväll. Nästa
schemalagda körning tar den.

**MILSTOLPE upplåst:** Efter API-004 (denna körning) är hela
MVP-flödet PREPARED end-to-end:
gäst-skanna-QR → se-nota (KI-002/API-002) → välja-Swish (KI-003) →
betala (API-003 + KI-003) → POS-uppdaterad (API-004 ← nytt nu).
Allt awaiting Zivar's `pnpm install && supabase db push && pnpm dev`
på hennes maskin.

## Progress

- Processed: **14/28** (4 done + 10 prepared).
- Target: 28.
- Remaining: 14 (att processa: POS-002, API-006, DB-003, KI-004, KI-005,
  KI-007 — plus de 7 skipped som landar direkt utan arbete +
  SA-001/TA-001-005 som behöver sekret/beslut).

## Anteckningar

- Context-användning denna körning: tight efter summary-rebuild men
  tillräckligt för 1 brief inkl. design-review + .prepared.md +
  NIGHT-RUN-update + status-fil. Inga sub-agents anropade — höll det
  i huvudtråden för bättre flöde.
- Ingen ny auto-memory skriven denna körning — regeln
  "en brief per körning" är redan sparad i
  `.auto-memory/feedback_one_brief_per_run.md` och respekterades.
- API-004 är 🔴 Think hard-brief ("HALVA produkten" — utan POS-update
  försöker servitören ta betalt igen). Extra omsorg lades på:
  idempotency vid storlås-nivå (UNIQUE-constraint + ON CONFLICT DO
  NOTHING), parallel-safety (FOR UPDATE SKIP LOCKED), crash-recovery
  (lease-expiry → orphan-claim), och defense-in-depth (DB-trigger som
  enqueue-chokepoint så ingen app-kod kan glömma). Se .prepared.md §
  "Design review" för 12-punkts self-review.
- Commit-ordning rekommendation oförändrad — DB-002 → DB-001 →
  API-001 → API-002 → API-003 → KI-001 → KI-002 → KI-003 → API-004
  (API-004 sist eftersom den både kräver `payments`-tabellen från
  DB-002 och `pos_integrations` från POS-001's migration 004, plus
  konsumerar `@flowpay/pos-adapters`).
