# Status 2026-04-20 03:00

## Briefs klara denna körning

- **BRIEF-KI-001** — Gäst-PWA skeleton + QR-route. Se
  `briefs/done/BRIEF-KI-001.done.md`. Commit `40ba61d`, pushad till origin/main.
- **Bonus:** scaffoldade också `packages/schemas` (minimal Zod-schema-paket)
  som förutsättning för KI-001. Detaljer i `.done.md` under "Avvikelser".

## Blocked denna körning

- **BRIEF-IN-002** — Supabase + lokal dev-miljö. Oförändrat (kräver Docker +
  Zivars cloud-projekt). Se `briefs/blocked/BRIEF-IN-002.blocked.md`.

## Öppna frågor

- **Inter Variable-källa** (från UI-001 → KI-002). Guest CSS använder
  `system-ui`-fallback tills dess. Default-förslag:
  `@fontsource-variable/inter` i `apps/guest` dev-deps. Icke-blockerande,
  avgörs när KI-002 körs.

## Nästa brief i ordning

Efter KI-001 finns **ingen eligible brief** i körordningen — alla
kvarvarande Fas 0/1-briefs ligger på IN-002 direkt eller transitivt:

- **DB-001, SC-001, DB-002, DB-003** — beror på IN-002.
- **API-001** (Fastify) — Steg 2 i briefen läser `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`
  från env; kan teoretiskt scaffoldas utan riktig DB, men blir otestbart
  eftersom briefens verifiering kräver att hälsocheck-endpointen pingar
  Supabase. Funktionellt blockerad.
- **KI-002** (Visa nota med riktig data) — beror på API-001 + API-002.
- **POS-001** (Onslip-adapter) — behöver tenants-tabellen från DB-001.
- **TA-*** — behöver auth + DB.

**Slutsats:** sprinten är nu **blockerad på IN-002**. Första körningen som
startar när IN-002 är `done.md` kommer plocka DB-001.

Jag skapar INTE `STATUS-ALL-BLOCKED.md` ännu eftersom:
1. Ingen formell `.blocked.md` finns för de beroende briefs:en (bara IN-002).
2. Om IN-002 unblockas mellan körningar kan nästa agent direkt välja DB-001
   utan att behöva städa bort en all-blocked-fil först.

Om IN-002 fortfarande är blockerad om tre schemalagda körningar till,
lyfter nästa körning upp en formell `STATUS-ALL-BLOCKED.md` och pausar
schemat så du slipper brusiga "nothing to do"-statuses.

## Progress

**3/28 briefs klara** (IN-001, UI-001, KI-001). 1 blocked (IN-002). 0 nya
frågor. Gate G0 (fundament) saknar fortfarande IN-002 + DB-001 + SC-001.

## ETA till nästa gate (best guess)

- **Blockerat tills Zivar kör IN-002 lokalt.**
- När IN-002 är klar: DB-001 (~1 körning), SC-001 (~1 körning 🔴, kan ta 2),
  DB-002 (~1 körning). → Tre körningar efter IN-002 är G0 stängd.

## Anteckningar (sandbox-quirks, uppdaterade)

- `/tmp/payflow-git` är fortfarande oanvändbart mellan sessioner
  (permissions från tidigare `nobody:nogroup`-ägare). Tredje körningen i rad.
  **Rekommendation:** uppdatera `.agent/env.sh` → byt `/tmp/payflow-git` mot
  `$HOME/payflow-git`. Det är en 1-raders ändring och sparar ~30 s boot
  varje körning. Jag rör inte skriptet i denna brief (hörde till IN-* och
  jag körde KI-001), men flaggar det som fas 0-housekeeping.
- `pnpm install` vid root faller fortfarande på `unlink '_testfile'`. För
  isolerade paket-mappar fungerar `npm install --no-save --no-package-lock`
  plus manuell `ln -s` för workspace-deps. Det här är inte sustainable på
  sikt (t.ex. när pnpm-lockfile måste revideras) men duger för typecheck/lint/build
  i sandboxen.
- Inga nya `_tmp_*`-filer denna körning (redan gitignored).
