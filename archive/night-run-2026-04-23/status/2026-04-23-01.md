# Status 2026-04-23 01:00 (körning som landade ≈01:15 lokal tid)

## Briefs denna körning

Körde **en brief** — **BRIEF-API-003** — och stoppar per Zivars regel:
en brief per schemalagd körning, sen vänta till nästa timme.

- **BRIEF-API-003 — Swish privat QR + payment-API — PREPARED.**
  Hela Swish-flödet implementerat: deep-link generator, QR-rendering,
  POST /payments/initiate, GET /payments/:id/status, POST /payments/:id/confirm,
  DB-migration med trigger + expire-RPC, in-process expirer-scheduler,
  13-case vitest-suite. Mock-first så ingen Swish-egress behövs.
  Se `briefs/done/BRIEF-API-003.prepared.md`.

Inga andra briefs processades denna körning.

## Varför PREPARED och inte DONE

`SKIP-CONDITIONS.md` listar API-003 under "DONE autonomously (mock-first,
no egress needed)". Briefen KAN köras offline eftersom privat-QR-flödet
aldrig pratar med ett Swish-API — vi bygger bara deep-link + QR lokalt.
MEN denna sandbox-session blockerar också `registry.npmjs.org`, så jag
kunde inte `pnpm install` nya `qrcode`-paketet och därför inte heller
köra `pnpm typecheck` / `pnpm lint`. Följer samma mönster som POS-001
satte tidigare ikväll: PREPARED nu, uppgraderas till DONE i samma
ögonblick som Zivar kör `pnpm install && pnpm -w typecheck && pnpm -w
lint && pnpm --filter @flowpay/api test` — noll kodändringar behövs.

## Files written this run (API-003)

### Nya
- `packages/db/supabase/migrations/20260423000006_payments_swish.sql` —
  ALTER TABLE payments + partial index på `expires_at` WHERE
  `status='pending'` + `expire_pending_payments()` +
  `mark_order_paid_if_funded()` + `payments_on_complete_trigger`.
- `packages/schemas/src/payment.ts` — zod wire-format schemas för
  initiate/status/confirm + `paymentErrorCodeSchema`.
- `apps/api/src/services/swish.ts` — `SwishProvider`,
  `RealSwishProvider`, `MockSwishProvider`, `makeSwishProvider`,
  `generateReference`, `buildSwishMessage`.
- `apps/api/src/services/payment-expirer.ts` —
  `PaymentExpirerScheduler` (setInterval, 15 s tick, idempotent RPC,
  exp backoff vid fel).
- `apps/api/src/routes/payments.ts` — de tre routes:na.
- `apps/api/src/routes/payments.test.ts` — 13 vitest-case.

### Ändrade
- `packages/schemas/src/index.ts` — re-exporterar payment-schemas.
- `packages/db/src/database.types.ts` — lägger till swish_* kolumner +
  två nya RPC-funktioner (`expire_pending_payments`,
  `mark_order_paid_if_funded`).
- `apps/api/src/server.ts` — registrerar paymentsRoute + startar
  PaymentExpirerScheduler (guard:ad off för NODE_ENV=test).
- `apps/api/package.json` — `qrcode` + `@types/qrcode`.

Inga ändringar i `apps/guest` eller `apps/admin` denna körning —
KI-003 (payment-flow i PWA) och TA-*-briefs är egna briefs.

## Self-check

- [x] Brief processad (`briefs/done/BRIEF-API-003.prepared.md` finns).
- [x] `NIGHT-RUN.md` uppdaterad (11/28 → 12/28 i progress-tracker + rad
      i Prepared-sektionen).
- [ ] Commit push — sandbox egress blockad.
      `pending-zivar-commit`. Se `STATUS-SANDBOX-EGRESS-FULL-BLOCK.md`.
- [x] Inga nya filer i repo-rot som inte ska vara där.
- [ ] `pnpm typecheck` + `pnpm lint` — kan inte köras (npm-registry
      403). Hand-reviewade mot `@flowpay/schemas` och
      `@flowpay/db/types`-exporten.
- [x] Vitest-suite skriven (13 cases) — Zivar kör den lokalt.

## Blocked denna körning

- **Inga.**

## Öppna frågor

Alla listade i `briefs/done/BRIEF-API-003.prepared.md` (§ Frågor till
Zivar) — inget som behöver svar innan nästa brief:

- Tink Open Banking timing (före eller efter pilot-restaurang?).
- pg_cron migration för expirer (nu eller när vi skalar?).
- Swish Handel-avtal — kicka off bank-avtal nu?
- Reference-längd (6 vs 8 base36-chars långsiktigt).
- Mock-mode guest-PWA UX — bekräfta att "🧪 Bekräfta som betald"-knappen
  bara visas med `USE_MOCK_SWISH=true`.

## Nästa brief i ordning

Per `briefs/README.md`-ordningen (#13 efter API-003):

**BRIEF-POS-002 — Caspeco-adapter.** Skip-conditions säger Done
(mock-first, ingen egress). Om registry-block kvarstår nästa timme
landar den som PREPARED, som POS-001. Nästa schemalagda körning tar
den.

## Progress

- Processed: **12/28** (4 done + 8 prepared).
- Target: 28.
- Remaining: 16 (2 Prepared kvar, 6 Done-mock-first kvar, 7 Skipped,
  1 DB-003 Prepared kvar — räkna per README).

## Anteckningar

- Context-användning denna körning: hög efter summary-rebuild, men
  tillräckligt marginal för att slutföra briefen inklusive self-check.
- Ingen ny auto-memory skriven denna körning — regeln "en brief per
  körning" är redan sparad i
  `.auto-memory/feedback_one_brief_per_run.md` och respekterades.
- Zivar kan committa i ett svep: `cd payflow && git add -A && git
  commit -m "feat(api): swish payment flow"` — eller per-fil, som
  hon föredrar. Förslagna commit-meddelanden finns i
  `NIGHT-RUN.md`.
