# Status — 2026-04-23 04:12+02:00 (hour 04)

## What ran

Scheduled task **flowpay-night-run-hourly**, picking up the next eligible brief per the "one brief per scheduled run, then stop" rule.

## Brief processed

**BRIEF-KI-005 — Dricks-selector 🟡** → **PREPARED** → `briefs/done/BRIEF-KI-005.prepared.md`

Suggested commit: `feat(guest,api,db): tip selector + restaurant tip config`

### What changed (files)

**New**
- `packages/db/supabase/migrations/20260423000008_restaurant_tip_config.sql` — adds `restaurants.default_tip_percent NUMERIC(5,2) DEFAULT 0` + `restaurants.tip_options JSONB DEFAULT '[0, 5, 10]'::jsonb`, two CHECKs (range [0,30] + array shape), and `CREATE OR REPLACE FUNCTION get_order_by_token` widening the projection with the two new columns.
- `apps/guest/src/components/TipSelector.tsx` — controlled component, ~430 LOC. Preset buttons (`role="radio"`, 64px min height, 0% rendered identically to others), custom-% input clamped at `TIP_CUSTOM_MAX_PERCENT = 30`, animated total preview with `AnimatePresence mode="wait"` keyed on `totalWithTip`, `useReducedMotion()` respected. Exports pure `computeInitialTipAmount()` so the parent route seeds its tip state with the exact same logic — no mount-useEffect.

**Modified**
- `packages/schemas/src/order.ts` — adds `tipPercentSchema` (z.number().nonnegative().max(30)) and `tipOptionsSchema`; extends `orderRestaurantPublicSchema` with `defaultTipPercent` + `tipOptions`.
- `apps/api/src/routes/orders.ts` — imports new types; adds `coerceTipPercent()` (fallback to 0 on NaN/undefined) and `coerceTipOptions()` (fallback to `[0, 5, 10]`, drops out-of-range elements); wires both into the response shape.
- `apps/api/src/routes/orders.test.ts` — updates `goodRow()` with default tip fields + tightens happy-path matcher; adds two new tests (fallback to defaults when null, coerce stringified numbers + drop garbage).
- `apps/guest/src/routes/payment.tsx` — adds `tipAmount` / `tipInvalid` state, seeds via `computeInitialTipAmount()` once order loads, renders `<TipSelector>` between the amount card and `<PaymentMethodSelector>` in the 'select' phase, passes `tip_amount: tipAmount` into the initiate mutation, disables Betala while `tipInvalid || tipAmount === null`.
- `apps/guest/src/components/PaymentMethodSelector.tsx` — adds a `disabled` prop separate from `isSubmitting` so the tip-invalid path doesn't flash "Startar…" on a greyed button.

### Why PREPARED (not DONE)

Sandbox egress to `github.com`, `registry.npmjs.org`, and `*.supabase.co` is all 403-blocked (see `STATUS-SANDBOX-EGRESS-FULL-BLOCK.md`). I can't `pnpm install` to run typecheck/lint, nor apply migration 008 against the live DB to confirm the widened RPC round-trips on supabase-js. Code was hand-reviewed against existing schemas + UI exports and reads clean; Zivar's local run upgrades this to DONE with no changes expected.

### Self-check

- [x] Brief processed correctly (`briefs/done/BRIEF-KI-005.prepared.md` exists)
- [x] `NIGHT-RUN.md` updated (counter 15 → 16 processed; KI-005 added to Prepared section)
- [ ] Commit pushed to `main` — **blocked by egress**, deferred to Zivar (consistent with every other prepared brief tonight)
- [x] Context budget OK (~60% used heading into status-write)
- [x] No new files in repo root
- [ ] `pnpm typecheck` + `pnpm lint` — deferred (no node_modules in sandbox; same as every other prepared brief tonight)

### Next brief (per queue)

Per `briefs/README.md` order, the next eligible brief is **BRIEF-POS-002 — Catapos adapter** (🔴). Leaving it for the next hourly run per the "one brief per scheduled run" rule.

## Progress

- Done: 4/28
- Prepared: 12/28 (was 11 at start of hour)
- Skipped: 0/28
- Blocked: 0/28
- **Total processed: 16/28**

## Open questions for Zivar

See `briefs/done/BRIEF-KI-005.prepared.md` → "Frågor till Zivar":

1. Do we want to raise the 30% tip cap for fine-dining / large parties?
2. Should we start adding Vitest + RTL tests for guest-side components? (TipSelector has pure helpers ready to cover.)
3. TA-004 (admin tip-config UI) writes to the two columns added here — no contract changes needed when it lands.

## End of run

Stopping after one brief per the schedule rule. Next hourly run picks up POS-002.
