# Status 2026-04-20 02:00

## Briefs klara denna körning

- **BRIEF-UI-001** — Designsystem + baskomponenter. Se `briefs/done/BRIEF-UI-001.done.md`.
  Commit `5c33af2`, pushad till origin/main.

## Blocked denna körning

- **BRIEF-IN-002** — Supabase + lokal dev-miljö. Oförändrat från förra körningen.
  Kräver Zivars Windows-maskin (Docker + cloud-projekt). Se
  `briefs/blocked/BRIEF-IN-002.blocked.md`.

## Öppna frågor

- **UI-001 → KI-001 overflow:** Inter Variable-källa (npm `@fontsource/inter-variable`
  vs Google Fonts vs eget asset). Default: `@fontsource/inter-variable`.
  Icke-blockerande, beslutet tas i KI-001. Nämnt i `BRIEF-UI-001.done.md`.

## Nästa brief i ordning

**BRIEF-DB-001** (Initial schema) står näst i körordningen men är **blockerad**
— den beror på IN-002 (Supabase). Samma gäller SC-001, DB-002, DB-003.

Nästa **eligible** brief i körordningen efter UI-001 (lista i beroende-ordning):

- **BRIEF-API-001** (Fastify-skeleton) — beror på IN-001 ✓, IN-002 ✗ → blockerad.
- **BRIEF-KI-001** (Gäst-PWA grund) — beror sannolikt på UI-001 ✓ och eventuellt
  API-001. Om bara UI-001 → eligible nu. Jag läser briefen i nästa körning
  för att bekräfta beroenden.

Om KI-001 kräver API-001 (och därmed IN-002) är **alla kvarvarande briefs
blockerade** tills Zivar kör Supabase-setupen lokalt. I så fall skapar nästa
körning `STATUS-ALL-BLOCKED.md`.

## Progress

**2/28 briefs klara** (IN-001, UI-001). 1 blocked (IN-002).

Nästa gate (G0 — fundament): behöver IN-002, DB-001, SC-001 klart.
IN-002 ligger på Zivar.

## ETA till nästa gate (best guess)

- **Blockerat tills Zivar kör IN-002 lokalt** (cloud-projekt + `supabase start` via Docker Desktop).
- Så fort IN-002 är klar kan agenten på egen hand köra DB-001 → SC-001 → (parallellt) UI-001 ✓
  → DB-002. Uppskattning: 3–4 körningar efter att IN-002 är unblocked.

## Anteckningar (sandbox-quirks)

- Fick installera pnpm via `npm install -g pnpm@10.33.0 --prefix $HOME/.npm-global`
  (inget persistent pnpm mellan sessioner).
- `pnpm install` mot root-mount fallerar fortfarande med EPERM/EACCES mot den
  låsta `node_modules/` från tidigare session. Workaround: `npm install --no-save`
  i `packages/ui/` bara för att få typecheck/lint att kunna resolve deps i sandbox.
  `packages/ui/node_modules` är `.gitignore`:d — Zivar kör `pnpm install` lokalt
  som vanligt.
- `GIT_DIR` flyttad till `$HOME/payflow-git` (samma workaround som IN-001).
  Om samma symptom återkommer för tredje körningen: uppdatera `.agent/env.sh`
  att använda `$HOME` istället för `/tmp`. Noterar det här men rör inte
  skriptet i denna brief.
- `pnpm-workspace.yaml` blev tillfälligt modifierad av en felriktad `pnpm config`
  (lade till `storeDir`-nyckel) — revertad innan commit.
