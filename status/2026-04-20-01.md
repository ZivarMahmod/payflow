# Status 2026-04-20 01:00 (första schemalagda körningen)

## Briefs klara denna körning
- BRIEF-IN-001 — Monorepo setup (pnpm + Turborepo). Commit `012ac0d`.

## Blocked denna körning
- BRIEF-IN-002 — Supabase + lokal dev-miljö. Sandboxen har ingen Docker och
  cloud-projektet kräver dina credentials. Se
  `briefs/blocked/BRIEF-IN-002.blocked.md` för exakt vad du behöver göra.

## Öppna frågor
Inga formella `questions/*.question.md` — alla avvikelser i IN-001 var
defensiva defaults (pnpm store-dir flyttad, biome.json auto-genererad,
GIT_DIR till `$HOME` istället för `/tmp`). Se avvikelse-sektionen i
`briefs/done/BRIEF-IN-001.done.md`.

## Nästa brief i ordning
**BRIEF-UI-001** — Designsystem + baskomponenter (`packages/ui`).
Beror endast på IN-001 → eligible nu. Nästa körning plockar upp den.

(IN-002 blockerar DB-001, SC-001, DB-002, DB-003. När du unblockat IN-002
lokalt kan hela Phase 0 fortsätta i ordning.)

## Progress
1 / 28 briefs klara (~3.6%). Nästa gate: **G0 — Grunden** (IN-001, IN-002,
DB-001, SC-001, UI-001). Saknas: IN-002 (blocked), DB-001, SC-001, UI-001.

## ETA till nästa gate (best guess)
- Om du unblockar IN-002 inom ett dygn: 4–5 körningar till G0 klar
  (UI-001 nästa körning; DB-001 + SC-001 följer när IN-002 är unblockat).
- Om IN-002 förblir blockerat: UI-001 hinner klart, sedan går körningarna
  in i `STATUS-ALL-BLOCKED` eftersom Phase 1 förutsätter DB.

## Sandbox-egenheter (för framtida körningar)
- `/tmp/payflow-git` kan äga sig av `nobody:nogroup` från tidigare session
  och blockera `setup-git.sh`. Lösning: `export PAYFLOW_GITDIR="$HOME/payflow-git"`
  före `setup-git.sh` körs. Långsiktig fix: uppdatera `.agent/env.sh` att
  default:a till `$HOME/payflow-git` (inte `/tmp`).
- pnpm default-store inne i mount → `EPERM` på `_tmp_*` unlink.
  Fix: `pnpm config set store-dir $HOME/.pnpm-store` (gjort, persisteras
  i user-config).
- Mount kan inte unlinka vissa filer (`_tmp_*`, `.symlink-test`).
  Lagda i `.gitignore`. Zivar kan radera från Windows-sidan när som helst.
