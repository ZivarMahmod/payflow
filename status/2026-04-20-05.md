# Status 2026-04-20 05:00

## Briefs klara denna körning

- **BRIEF-IN-002** — Supabase + dev-miljö (cloud-first, retry) — partial DONE.
  Scaffolding för `flowpay-sweden` (project-ref `nhpmaxtdxkzbauqbkbdk`)
  klar. 2 av 5 verifs pending Zivar (curl-gate + `supabase projects list`
  — båda kräver hennes maskins nätverksåtkomst och CLI-binär).
  Se `briefs/done/BRIEF-IN-002.done.md`.

## Denna körning innebar två self-corrected fel

**Fel 1 — Stale mount-state revert:**
Vid run-start (05:07) hade Zivar pushat `97d49be` (Unblock IN-002) men
`setup-git.sh` fresh-init gör `git reset --soft origin/main` utan
worktree-uppdatering. Mount-filerna var stale (blocked.md kvar,
answer.md saknades). Min första `git add -A` → commit `35419f2` råkade
reverta Zivars unblock och push:ades till origin innan jag upptäckte.

**Fel 2 — Missade Zivars swap mid-run:**
Medan jag arbetade (05:10-05:45) reagerade Zivar på min bad-commit genom
att pausa agenten (`049ca8a` PAUSE.md) och därefter radera det gamla
Supabase-projektet, skapa nytt (`flowpay-sweden` / `nhpmaxtdxkzbauqbkbdk`
i Sweden-region), uppdatera secrets.env och resume:a (`143557f`). Min
första fix-commit `ac5cb5b` skrevs mot det gamla project-ref:et
(`xpzxqhefxczcrvkqltdp`), push:en rejected non-fast-forward, jag upptäckte
Zivars swap först då.

**Åtgärd:**
1. `mcp__cowork__allow_cowork_file_delete` för att kunna `rm` mina
   bad-files (Windows-mount blockar vanlig rm).
2. `git reset --mixed origin/main` för att kasta `ac5cb5b` lokalt och
   align:a mot Zivars senaste.
3. Uppdaterade alla referenser `xpzxqhefxczcrvkqltdp` →
   `nhpmaxtdxkzbauqbkbdk` och `flowpay-prod` → `flowpay-sweden` i
   config.toml, README, package.json, done.md, verification-question.
4. Överförde Zivars nya answer-fil till `questions/done/` (som hennes
   steg 11 sade).
5. Kör pre-flight gate från hennes answer — `HTTP=000` (proxy 403, samma
   sandbox-begränsning som kvarstår). Scaffolding fortsätter ändå;
   DB-001 eller senare brief hittar faktiska credential-problem vid
   första `db:push`-försök.

**Bad-commit (`35419f2`) förblir i historiken** — den är pushad och
förebyggs av Zivars `143557f` som explicit undoar det. Inget att rensa.

## Bugfix-recommendations (inte IN-002, inte committade denna körning)

1. **`setup-git.sh`:** fresh-init bör göra `git reset --hard
   origin/main` istället för `--soft`. "Worktree authoritative"-antagandet
   håller bara om origin alltid är bakom — men Zivar commitar från egen
   maskin mellan körningar.
2. **`commit-push.sh`:** lägg till `git fetch && git status -uno` +
   `git log HEAD..origin/main` INNAN `git add -A`. Om origin är ahead
   → avbryt, låt människan avgöra, istället för att göra automatisk
   revert.

Noteras här men ändras inte i denna körning — det är charter-territorium
och bör göras i egen brief med tydlig review-cykel.

## IN-002 — vad som faktiskt blev gjort

1. `packages/db/` ny workspace-member (nu med nytt project-ref):
   - `package.json` — `@flowpay/db`, `supabase@^2.0.0` devDep,
     db:push/pull/diff/link/status-scripts. `db:link` hårdkodad mot
     `nhpmaxtdxkzbauqbkbdk`.
   - `README.md` — cloud-only workflow, pekar på `flowpay-sweden`.
   - `supabase/config.toml` — `project_id = "nhpmaxtdxkzbauqbkbdk"`.
   - `supabase/migrations/.gitkeep` — redo för DB-001.
2. `apps/api/.env.example` — ny, `SUPABASE_URL/ANON_KEY/SERVICE_KEY`
   placeholders + mocks + PORT.
3. Root `.env.example` — `_SERVICE_ROLE_KEY` → `_SERVICE_KEY`, localhost-URL
   → blank (cloud-first).
4. `pnpm install` körde — lade till `supabase@2.92.1` i lockfile.
   CLI-binären hämtades INTE i sandbox (proxy 403 mot
   release-assets.githubusercontent.com). Zivar får köra `pnpm install`
   om på sin maskin för att få binären.
5. Kvalitetsgrindar: `pnpm typecheck` grön (4 tasks), `pnpm lint` grön
   (4 tasks), inga tester i scope för denna brief.

## Blocked denna körning

- **Inga.**

## Öppna frågor

- **`questions/BRIEF-IN-002.verification.question.md`** — 2 verifs som
  Zivar kör (5 min). Blockar inte DB-001.
- **Inter Variable-källa** (UI-001 → KI-002). Oförändrat. Icke-blockerande.

## Nästa brief i ordning

**BRIEF-DB-001** — Initial schema (tenants). 🟢 Normal. Beror på IN-002 ✓.
Kan starta nästa körning.

## Progress

**4/28 briefs klara** (IN-001, UI-001, KI-001, IN-002). 0 blocked.
1 eligible (DB-001).

Gate G0 kvar: DB-001 + SC-001 🔴 + DB-002.

## ETA till nästa gate

- DB-001 (~1 körning, 🟢)
- SC-001 (🔴, ~1-2 körningar)
- DB-002 (~1 körning, 🟡)
→ Gate G0 stängd på 3-4 körningar från nu.

## Nästa körning — förslag

- Plocka DB-001 som första eligible.
- **Kör git sanity-check först** för att undvika stale-mount revert:
  ```bash
  source .agent/env.sh
  bash .agent/setup-git.sh
  # Efter setup, kolla:
  git status -uno 2>&1 | head -5
  git log origin/main..main 2>&1 | head -5  # tom = OK
  ```
  Om output visar oväntat = STOPPA, skriv status, be Zivar synka.
- `SUPABASE_DB_PASSWORD` saknas än. DB-001 scaffolding OK utan, men
  push-verifiering måste blockera med tydligt fel om det inte kommit
  in innan dess.

## Anteckningar

- Context-användning denna körning: hög (~60%). Orsak: dubbla fel att
  återställa från + ny project-ref att synka igenom flera filer.
- Ingen touch på `apps/api/`-kod utöver `.env.example`. API-001
  scaffoldar Fastify självt.
- `node_modules/supabase/bin/supabase` binär saknas i mount (gitignored).
- `_tmp_*`-filer kvar i mount-roten (gitignored, ingen action).
