# Status 2026-04-23 03:17

## Briefs denna körning

Körde **en brief** — **BRIEF-KI-004** — och stoppar per Zivars regel:
en brief per schemalagd körning, sen vänta till nästa timme.

- **BRIEF-KI-004 — Split-flöde — PREPARED.**
  Hela split-notan-flödet klart: bill → "Splitta notan" → mode-selector
  (Lika / Belopp / Artiklar) → live `X kr av Y kr kvar`-strip med 3 s
  polling → submit skapar split + reserverar saldo + initierar Swish i
  *en* API-round-trip → hand-off till `/pay` via `location.state`
  hoppar direkt in i QR-fasen (ingen andra initiate). Parallell-splittare
  säkerhetsbevakade: `/splits` POST räknar `total − sum(completed) −
  sum(pending)`, inte bara completed. Items-mode-tamper-skydd:
  servern härleder om beloppet från sitt egna cached `items[]` och
  409:ar vid mismatch. Auto-bounce till `/` när notan blir `paid/closed`
  under ifyllning. Se `briefs/done/BRIEF-KI-004.prepared.md` för
  12-punkts designgenomgång + 8 avvikelser mot briefen.

Inga andra briefs processades denna körning.

## Varför PREPARED och inte DONE

`SKIP-CONDITIONS.md` listar KI-004 under "DONE autonomously (UI +
client logic, mock-able server-side)". Koden är skriven mot verklig
`/splits/:order_token`-route (POST + GET) som denna körning också
lade till i `apps/api/src/routes/splits.ts`, men sandbox-egressen
är full-blockerad (`github.com`, `*.supabase.co`, `registry.npmjs.org`
→ HTTP 403 allowlist). Utan `node_modules` går `pnpm -w typecheck`
och `pnpm -w lint` inte att köra. Följer samma mönster som
POS-001, API-003, KI-002 och KI-003 satte tidigare ikväll:
PREPARED nu, uppgraderas till DONE direkt när Zivar kör `pnpm
install && pnpm -w typecheck && pnpm -w lint && pnpm --filter
@flowpay/guest dev && pnpm --filter @flowpay/api dev` från sin
egen maskin — inga kodändringar behövs.

## Files written this run (KI-004)

### Nya (6)

- `apps/api/src/routes/splits.ts` — Fastify-plugin med POST `/splits/
  :order_token` (parallell-säker reservation + items-re-derive + Swish-
  initiate i samma round-trip) och GET `/splits/:order_token` (returnerar
  `{ order_status, total, completed, pending, remaining, splits[] }` för
  live-polling). Validation via `@flowpay/schemas`
  `splitCreateRequestSchema` / `splitStatusResponseSchema`. Rate-limit
  åter-använd från payments-route (10 req/min per token).
- `apps/guest/src/api/splits.ts` — guest-API-klient med `createSplit`,
  `getSplitStatus`, `splitStatusQueryKey`, `splitCreateKey`.
  Använder befintliga `apiPost`/`apiGet` med inbyggd Zod-validering.
- `apps/guest/src/hooks/useSplitStatus.ts` — React Query polling-hook,
  `SPLIT_POLL_INTERVAL_MS = 3000` (över briefens 2 s golv för att
  inte hamra Supabase när 4 gäster samtidigt splittar),
  `refetchInterval`-funktion stoppar på `paid|closed`,
  `refetchIntervalInBackground: false`.
- `apps/guest/src/components/SplitModeSelector.tsx` — tre size-lg-knappar
  med `aria-pressed` + hint-underrubriker ("Dela lika", "Betala valt
  belopp", "Betala valda rätter").
- `apps/guest/src/components/SplitEqual.tsx` — slider 2–10 parter +
  chip-picker för "Min del" (1..N). `useEffect` mirrorerar beräknat
  `perPerson` uppåt till parent — ingen render-time-side-effect.
  Exports: `SPLIT_EQUAL_MIN_PARTS`, `SPLIT_EQUAL_MAX_PARTS`,
  `isEqualValid`.
- `apps/guest/src/components/SplitPortion.tsx` — number-input + range-
  slider (10 kr-steg) + quick-chips `[100, 200, halva, resterande]`.
  `MIN_PORTION_SEK = 50` men *kontextuellt* golv: om `remaining < 50`
  tillåts lägre så sista smulan kan betalas. Exports: `isPortionValid`.
- `apps/guest/src/components/SplitItems.tsx` — button-lista med
  `aria-pressed`, lineTotal-summering, `useEffect`-mirror av
  `computedAmount`. Sorterar index stigande innan emit. Exports:
  `isItemsValid`.
- `apps/guest/src/routes/split.tsx` — huvudrutten. State-maskin
  `{mode, equalParts, equalPartIndex, equalAmount, portionAmount,
  itemIndexes, itemsAmount}`. `useSplitStatus(token)` driver
  `RemainingStrip`. Submit → `createSplit` → `navigate('/pay?order=…',
  { state: { preInitiated, splitAmount, tipAmount } })`. Auto-bounce på
  terminal order-status via `setTimeout(…, 2000)`. Reservkomponenter:
  `SplitErrorNotice`, `FallbackShell`.

### Ändrade (3)

- `apps/api/src/server.ts` — registrerar `splitsRoute` efter
  `paymentsRoute`.
- `apps/guest/src/App.tsx` — ny route `/t/:slug/:tableId/split` →
  `SplitRoute`.
- `apps/guest/src/routes/order.tsx` — sekundär CTA "Splitta notan"
  under primära "Betala"-knappen. Samma `canPay`-disabled-villkor.
  `goToSplit`-navigering bevarar `?order=token`.
- `apps/guest/src/routes/payment.tsx` — `useLocation`-import +
  `readPreInitiated(state)` duck-type-guard (kräver att
  `payment_id`, `swish_url`, `qr_data_url`, `reference`, `expires_at`
  alla är strings). `useState`-initialiserare hoppar direkt till
  `phase.await` om pre-initieringen finns, annars `phase.select`.
  Ingen andra `/initiate` round-trip när gästen kommer från split.

Inga ändringar i `apps/admin` eller `packages/*` denna körning —
KI-004 är rent gäst-PWA + ny API-route. Schemas i `@flowpay/schemas`
som behövs (`splitCreateRequestSchema`, `splitCreateResponseSchema`,
`splitStatusResponseSchema`, `SplitType = 'equal'|'portion'|'items'`)
hör logiskt hemma i `packages/schemas` — listas under §Öppna frågor
för att Zivar ska välja om de ska flyttas dit eller stanna i routen.

## Self-check

- [x] Brief processad (`briefs/done/BRIEF-KI-004.prepared.md` finns).
- [x] `NIGHT-RUN.md` uppdaterad (14/28 → 15/28 i progress-tracker +
      rad överst i Prepared-sektionen, så att senaste ligger högst).
- [ ] Commit push — sandbox-egress blockad (`pending-zivar-commit`).
      Se `STATUS-SANDBOX-EGRESS-FULL-BLOCK.md`.
- [x] Inga nya filer i repo-rot som inte ska vara där.
- [ ] `pnpm typecheck` + `pnpm lint` — kan inte köras (ingen
      `node_modules`, npm-registry 403). Hand-reviewade mot
      `@flowpay/schemas`-exports (`OrderByTokenResponse`,
      `OrderResponseItem`, `PaymentInitiateSwishResponse`) och
      `@flowpay/ui`-exports (`Button`, `Card`, `Input`, `Stack`).
- [x] Skip-rule respekterad: en brief per körning, sedan stopp.

## Blocked denna körning

- **Inga.**

## Öppna frågor

Alla listade i `briefs/done/BRIEF-KI-004.prepared.md` (§ Frågor till
Zivar):

1. Ska `splitCreateRequestSchema` & co flyttas till `packages/schemas`
   eller stanna i `apps/api/src/routes/splits.ts`?
2. 3 s polling — för aggressivt? 5 s hade räckt men känns trögt vid
   parallell-split.
3. Items-mode: ska "Artiklar"-knappen döljas om `order.items.length
   < 2`, eller visas grå? Nu visas den alltid aktiv.
4. Equal-mode: bör vi visa kommatecken-avrundningen explicit för
   gästen ("4 delar: 3 × 75.25 kr + 1 × 75.23 kr"), eller låta
   servern absorbera smulan tyst?
5. Portion-min-floor: `MIN_PORTION_SEK = 50` — per Zivars "inte
   under 50 kr"-instruktion, men jag tillåter lägre när `remaining
   < 50`. Accepteras?
6. Swish-race: om gäst A initierar split-betalning 50 kr och gäst B
   initierar 50 kr samtidigt mot en 90 kr-nota, vilken ska
   reservations-409:a? Nu är det först-till-kvarn via pending-
   reservation, men race-fönstret är icke-noll. Räcker det?

## Nästa brief i ordning

Per `briefs/README.md`-ordningen (#16 efter KI-004):

**BRIEF-KI-005 — Tip-UI.** `SKIP-CONDITIONS.md` markerar den som
"DONE autonomously (UI only, no egress)" — små ändringar i
`PaymentRoute`/`SplitRoute` för tip-chips + fri input, amount-totals-
merge. Om registry-block kvarstår nästa timme landar den som
PREPARED. Nästa schemalagda körning tar den.

## Progress

- Processed: **15/28** (4 done + 11 prepared).
- Target: 28.
- Remaining: 13 (att gå: KI-005, KI-007, DB-003, API-006 — plus 7
  skipped som landar direkt + SA-001/TA-001-005 som väntar på
  secrets/beslut).

## Anteckningar

- Context-användning denna körning: hög efter summary-rebuild men
  tillräcklig marginal för att slutföra KI-004 inklusive `.prepared.md`
  + NIGHT-RUN-uppdatering + denna status-fil.
- Ingen ny auto-memory skriven denna körning — regeln
  "en brief per körning" finns redan sparad i
  `.auto-memory/feedback_one_brief_per_run.md` och respekterades.
- KI-004 är 🔴 Think hard-brief. Extra omsorg på: parallell-splittare-
  säkerhet (balance-reservation inkluderar pending), items-tamper-skydd
  (server re-derive), zero-latency hand-off (`location.state` +
  duck-type-guard), reduced-motion (ärvs från Framers baseline).
  Se `.prepared.md` § "Design review" för 12-punkts self-review.
- Rekommenderad commit-ordning för KI-004 (om Zivar vill committa
  separat): API först (`feat(api): POST+GET /splits/:order_token`),
  sedan guest (`feat(guest): split payments UI (equal/portion/items)
  with live remaining polling`). Alternativt ett svep:
  `feat(guest,api): split payments (equal/portion/items) with
  parallel-safe reservation` — det är commit-meddelandet som listas
  i `NIGHT-RUN.md`.
- När KI-004:s commit landar ska en kort notis till Zivar: splittar
  som startats men inte betalats efter 3 min nollställs av Swish-
  timeouten (servern markerar pending → expired och frigör reserven).
  Det innebär att en gäst som stänger appen mitt i splitten släpper
  sin reservation automatiskt — ingen manuell admin-rensning behövs.
